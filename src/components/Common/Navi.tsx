import { ReactNode } from "react";
import { NextPage } from "next";

export interface NaviProps {
  children?: ReactNode;
}

export const Navi: NextPage<NaviProps> = (data) => {
  return <div>tset</div>;
};

export default Navi;
