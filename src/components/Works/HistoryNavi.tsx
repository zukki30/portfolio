import { ReactNode } from "react";
import { NextPage } from "next";

export interface HistoryNaviProps {
  children?: ReactNode;
}

export const HistoryNavi: NextPage<HistoryNaviProps> = (props) => {
  return <div>tset</div>;
};

export default HistoryNavi;
